###########################################
# Vars
###########################################

# Terminal Emulator
set $terminal kitty

# Sets program names 
set $task_mgr btop
set $editor code

# Refreshes i3status
set $refresh_i3status killall -SIGUSR1 i3status

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"


###########################################
# i3-Specific
###########################################

# Reload config (+ Restarts i3)
bindsym $mod+Shift+r reload; restart

# Restarts i3
bindsym $mod+Ctrl+r restart

# Exits i3
bindsym $mod+Shift+q exec i3-msg exit

# Splits (horizontal, vertical)
bindsym $mod+h split h
bindsym $mod+v split v

# Change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# Toggle fullscreen on focused container
bindsym $mod+f fullscreen toggle

# Kills the focused container
bindsym $mod+q kill


###########################################
# Movement
###########################################

# Focus the parent container
bindsym $mod+a focus parent

# Move focus
bindsym $mod+Left focus left
bindsym $mod+Up focus up
bindsym $mod+Right focus right
bindsym $mod+Down focus down

# Move focused container
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Up move Up
bindsym $mod+Shift+Right move right
bindsym $mod+Shift+Down move down

# Resizes focused container
bindsym $mod+Ctrl+Left resize grow width 10 px or 10 ppt
bindsym $mod+Ctrl+Up resize grow height 10 px or 10 ppt
bindsym $mod+Ctrl+Right resize shrink width 10 px or 10 ppt
bindsym $mod+Ctrl+Down resize shrink height 10 px or 10 ppt

# Switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# Move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10


###########################################
# Controls
###########################################

# Changes the audio volume at pulseaudio
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Screenshot-Clipboard (Fullscreen, Selection, Active Win)
mode "screenshot-clipboard" {
    bindsym f exec --no-startup-id maim | xclip -selection clipboard -t image/png; mode "default"
    bindsym s exec --no-startup-id maim --select | xclip -selection clipboard -t image/png; mode "default"
    bindsym a exec --no-startup-id maim --window $(xdotool getactivewindow) | xclip -selection clipboard -t image/png; mode "default"
}
bindsym $mod+Print mode "screenshot-clipboard"

# Screenshot-Clipboard (Fullscreen, Selection, Active Win)
mode "screenshot-file" {
    bindsym f exec --no-startup-id maim "/home/$USER/Pictures/screenshot-$(date +%I:%M:%S-%e-%m-%y).jpg"; mode "default"
    bindsym s exec --no-startup-id maim --select "/home/$USER/Pictures/screenshot-$(date +%I:%M:%S-%e-%m-%y).jpg"; mode "default"
    bindsym a exec --no-startup-id maim --window $(xdotool getactivewindow) "/home/$USER/Pictures/screenshot-$(date +%I:%M:%S-%e-%m-%y).jpg"; mode "default"
}
bindsym $mod+Shift+Print mode "screenshot-file"


###########################################
# Program-Launches
###########################################

# Launches rofi
bindsym $mod+d exec --no-startup-id rofi -dpi 220 -show drun

# Launches Terminal (kitty)
bindsym $mod+Return exec $terminal

mode "launch" {
    bindsym t exec "$terminal -e $task_mgr"; mode "default"
    bindsym c exec $editor; mode "default"
}
bindsym $mod+l mode "launch"